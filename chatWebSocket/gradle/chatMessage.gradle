task runIdl() {
    println project.name

    doFirst() {
        println "start build all idl files..."
        def projectName = project.name
        fileTree(dir: "idl", include: '**/*proto').visit {File file ->
            println file
            def command = "protoc --proto_path=$projectName/idl " +
            "--java_out=$projectName/src/main/java " +
            "--php_out=$projectName/src/main/php " +
            "--js_out=$projectName/src/main/javascript $file"
            //println command
            //println command.execute().text
        }
    }
}